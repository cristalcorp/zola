# Zola on GitHub pages

All the installation and configuration process has been documented [here](https://rust.cristalcorp.com/zola/).
  
Note that if you juste want to clone it, you will have to initialize the theme submodule as follows :
```bash
git submodule update --init --recursive
```
